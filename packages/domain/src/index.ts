export type PortfolioSummary = {
  id: string;
  name: string;
  baseCurrency: string;
};

export const domainPackage = {
  name: "@dcalytics/domain",
  purpose: "Shared product types and core business rules."
};
